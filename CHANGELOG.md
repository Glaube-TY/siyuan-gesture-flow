# Changelog

## 0.1.0

### Features

- 自定义单段和复合手势（方向序列识别，4/8 方向模式）。
- 内置标签页、文档、滚动和导航操作：
  - 上一个 / 下一个标签页；
  - 关闭当前标签页；
  - 恢复最近关闭的标签页；
  - 重新加载当前文档；
  - 滚动到顶部 / 底部；
  - 后退 / 前进。
- 自定义快捷键动作与操作名称。
- 手势轨迹与动作提示。
- 绑定管理：新增、编辑、删除、启停。
- 配置导入、导出和重置。
- 浅色与深色主题适配。
- 普通右键兼容与临时禁用键。

### Notes

- 当前版本支持桌面端鼠标右键输入手势。
- JavaScript、触控板和侧键输入位于未来规划。

---

以下为开发过程记录（仅作归档，不再反映当前发布结构）。

### Pre-release hardening

- 修复删除绑定确认的 HTML 注入风险（自建安全确认弹窗，全部内容以 textContent 渲染）。
- 建立插件生命周期 generation token：异步配置加载、设置打开、订阅回调在卸载后一律失效，不会重新挂载监听器或 Canvas。
- ConfigManager 在异步加载期间被销毁时不再改写内存/磁盘、不再通知订阅者。
- 设置页在配置加载完成前打开时等待共享加载 Promise（复用同一次加载），不会用默认配置覆盖真实配置。
- 运行时重启失败并回滚时，将上一份可运行配置恢复回 ConfigManager，避免界面、磁盘与运行时三者分叉。
- 产品定位统一为"手势"，不再写死为"鼠标手势"。

### 历史功能记录（已并入首发）

- 快捷键：统一键规范化与校验、跨平台显示、合成事件执行（不伪造 isTrusted）。
- 配置：单一首发结构（version 1），开发期迁移体系已移除；不兼容的开发期配置在加载时恢复默认并覆盖保存。
- 识别：路径采样、RDP 简化、方向向量化与匹配；8 方向模式。
- 输入：桌面端鼠标右键（PointerEvent），含菜单保护与临时禁用键。
- 设置：自定义 Dialog 承载的 Svelte 设置页。
