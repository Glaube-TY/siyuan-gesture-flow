# Changelog

## 0.2.0

### Features

- 丰富内置功能，覆盖搜索、文档、侧栏与视图、布局和应用与系统等日常操作。
- 新增"搜索"组：全局搜索、搜索选中文本。
- 新增"文档"组：新建文档、打开今日日记、最近文档、数据历史、闪卡。
- 新增"侧栏与视图"组：文件树、大纲、反向链接、书签、标签、收集箱、文档关系图、全局关系图、Dock 栏。
- 扩展"标签页"组：关闭左侧、关闭右侧、关闭其他、关闭全部。
- 新增"布局"组：左右分屏、上下分屏、取消当前分屏、取消全部分屏。
- 新增"应用与系统"组：打开设置、立即同步、切换只读、锁定屏幕。
- 所有新增动作统一通过思源官方 `globalCommand` 执行，行为与思源原生功能保持一致。
- 设置页命令选择器按组展示，命令来自真实命令注册表。

### Notes

- 现有 v0.1.0 用户配置保持兼容，配置结构未变化，无需迁移。
- 默认手势保持不变：L / R / U / D。
- 新增动作均不建立默认手势。
- 无法读取的已有配置（损坏、版本过高或不兼容）不会被自动覆盖：插件临时使用默认设置运行并提示一次，磁盘上的原配置保持不变。

## 0.1.0

### Features

- 自定义单段和复合手势（方向序列识别，4/8 方向模式）。
- 内置标签页、文档、滚动和导航操作：
  - 上一个 / 下一个标签页；
  - 关闭当前标签页；
  - 恢复最近关闭的标签页；
  - 重新加载当前文档；
  - 滚动到顶部 / 底部；
  - 后退 / 前进。
- 自定义快捷键动作与操作名称。
- 手势轨迹与动作提示。
- 绑定管理：新增、编辑、删除、启停。
- 配置导入、导出和重置。
- 浅色与深色主题适配。
- 普通右键兼容与临时禁用键。
- "关于插件"页面（插件简介、作者与联系、支持与打赏；个人网站 / GitHub / 问题反馈 / 打赏入口）。
- 关于插件页面保持简洁，不显示需要额外维护的版本信息。
- 微信与支付宝打赏二维码（远程 HTTPS 图片，加载失败自动隐藏并保留打赏按钮）。
- funding 元数据（plugin.json custom + package.json custom）。
- 运行时控制台输出精简：正常操作保持静默，仅在真实异常时输出带 `[GestureFlow]` 前缀的 warn/error。
- 正式图标（160×160）与预览图更新。

### Notes

- 当前版本支持桌面端鼠标右键输入手势。
- JavaScript、触控板和侧键输入位于未来规划。
